# ===================================================================
# HARDPC BACKEND - APPLICATION PROPERTIES
# ===================================================================

# ===================================================================
# 1. CONFIGURACION GENERAL
# ===================================================================
spring.application.name=hardpc-backend-api

# ===================================================================
# 2. CONFIGURACION DE BASE DE DATOS (PRODUCCION / AIVEN CLOUD) - ACTIVO
# ===================================================================
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===================================================================
# 3. CONFIGURACION DE HIBERNATE / JPA (GLOBAL)
# ===================================================================
# Propiedad que ayuda a Hibernate a optimizar las consultas para el dialecto MySQL.
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# 'update': Actualiza el esquema de la BD sin eliminar los datos existentes (Ideal para MVP).
spring.jpa.hibernate.ddl-auto=update

# Mostrar y dar formato a las consultas SQL en la consola (util para depuracion del backend).
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===================================================================
# 4. GESTION DE ERRORES Y RUTAS WEB (API REST)
# ===================================================================
# Habilitar el lanzamiento de excepcion cuando no se encuentra un handler/endpoint (404).
spring.mvc.throw-exception-if-no-handler-found=true

# Deshabilitar el mapeo de recursos estaticos por defecto (necesario para que la propiedad anterior funcione estrictamente en APIs REST).
spring.web.resources.add-mappings=false


# ===================================================================
# ===================================================================
# SECCION DE ENTORNOS INACTIVOS (DESARROLLO LOCAL / PRUEBAS)
# Descomentar estas lineas unicamente si se necesita trabajar offline.
# ===================================================================
# ===================================================================

# --- A. ENTORNO LOCAL (MYSQL OFFLINE) ---
# spring.datasource.url=jdbc:mysql://localhost:3306/hardpc_db?serverTimezone=America/Lima
# spring.datasource.username=root
# spring.datasource.password=
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- B. ENTORNO DE PRUEBAS (H2 IN-MEMORY) ---
# Base de datos en memoria (volatil) ideal para testing unitario.
# spring.datasource.url=jdbc:h2:mem:hardpc_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# --- C. INICIALIZACION DE DATOS (SEEDING) ---
# Garantiza que Hibernate genere primero el esquema de la base de datos (DDL) antes de ejecutar scripts DML como data.sql.
# spring.jpa.defer-datasource-initialization=true